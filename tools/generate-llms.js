// Optional build step placeholder.
// This project references this script in the build pipeline; keep it present so
// CI/deploy builds don't fail when the feature isn't configured.

console.log('generate-llms: skipped');
